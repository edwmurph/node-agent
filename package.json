{
  "name": "node-agent",
  "description": "daemonset agent that runs on each k8s node",
  "private": true,
  "license": "UNLICENSED",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:debug": "eslint --debug --print-config index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@koa/router": "10.1.1",
    "http-errors": "2.0.0",
    "koa": "2.13.4"
  },
  "devDependencies": {
    "@edwmurph/eslint-config": "3.3.0"
  }
}
